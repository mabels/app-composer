{
  "name": "app-composer",
  "description": "top level app-composer",
  "version": "2.0.0",
  "private": true,
  "license": "MIT",
  "scripts": {
    "install": "lerna bootstrap && lerna run install",
    "build": "lerna run build --stream --include-filtered-dependencies",
    "pretest": "npm run build",
    "test": "lerna run test --stream --include-filtered-dependencies"
  },
  "devDependencies": {
    "lerna": "^3.6.0"
  }
}
